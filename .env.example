# Prebuilt image published by GitHub Actions to GHCR.
IMAGE_NAME=ghcr.io/jikssha/linuxdo-monitor

# Image tag to deploy. Use latest for the newest main build, or pin a tag/sha.
IMAGE_TAG=latest

# Host port exposed by docker compose for the web management UI.
WEB_BIND_PORT=8080
